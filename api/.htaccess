# Block direct browser access to PHP files except through AJAX
<FilesMatch "^(setup|db)\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Security headers for API responses
Header always set X-Content-Type-Options "nosniff"
Header always set X-Frame-Options "DENY"
